services:
  tests:
    image: buildkite/plugin-tester
    volumes:
      - ".:/plugin:ro"
  lint:
    image: buildkite/plugin-linter
    command: ["--id", "automattic/a8c-ci-toolkit"]
    volumes:
      - ".:/plugin:ro"
